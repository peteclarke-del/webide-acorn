/* The vendored ElkJS modules attach themselves to a global `ElkJs`, which
 * upstream creates in `elkjs.js`. That file is its user-interface entry point
 * and is deliberately not vendored, so the namespace is declared here instead. */
var ElkJs = {};
