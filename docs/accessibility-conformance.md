# WCAG 2.2 AA conformance checklist

This document is generated from the checklist the product maintains beside
its accessibility rules, and a contract fails the release gate whenever the
two stop matching. Regenerate it with `npm run conformance`.

A conformance claim is worth exactly as much as the evidence behind it, so
this records the evidence rather than the claim. Every Level A and AA
success criterion is listed with how it is verified.

## What the answers mean

- **Automated** — a rule in the release gate decides it, and the rule is named. It runs on every build.
- **Partly automated** — a rule decides part of it and the rest is manual. Both halves are stated rather than glossed.
- **Manual** — only a person can decide it, and what they have to do is stated.
- **Not applicable** — the product has no content the criterion applies to, with the reason. Each would stop being not applicable the day the product gained that kind of content.

Of 55 Level A and AA success criteria: 13 are decided automatically, 9 partly, 22 by a person, and 11 do not apply.

The distinction carrying the most weight is between automated and manual. A
scan can find a control with no accessible name; it cannot tell whether the
name means anything. Recording the second as though the first covered it is
how a product ends up believing it is accessible.

## Criteria

| Criterion | Name | Level | Verified | Evidence |
| --- | --- | --- | --- | --- |
| 1.1.1 | Non-text Content | A | Partly automated | The gate rules image-alt and visual-alternatives decide whether an alternative exists. Whether an alternative conveys what the visual conveyed is manual. |
| 1.2.1 | Audio-only and Video-only (Prerecorded) | A | Not applicable | The product ships no prerecorded audio or video. Emulator sound is generated by the machine being emulated and is a live output, not media content. |
| 1.2.2 | Captions (Prerecorded) | A | Not applicable | No prerecorded video is shipped with the product or its help, so there is nothing to caption. |
| 1.2.3 | Audio Description or Media Alternative (Prerecorded) | A | Not applicable | No prerecorded video is shipped, so there is nothing needing an audio description or a media alternative. |
| 1.2.4 | Captions (Live) | AA | Not applicable | No live audio or video is presented anywhere in the product; emulator sound is a machine output rather than media. |
| 1.2.5 | Audio Description (Prerecorded) | AA | Not applicable | No prerecorded video is shipped, so there is nothing that could carry an audio description. |
| 1.3.1 | Info and Relationships | A | Partly automated | The gate rules heading-order and landmark-label decide structure and naming. Whether the structure matches the visual grouping is manual. |
| 1.3.2 | Meaningful Sequence | A | Manual | Reading order is judged by a person moving through each workspace with a screen reader. No rule can decide whether an order makes sense. |
| 1.3.3 | Sensory Characteristics | A | Manual | Reviewed by reading every instruction for a reference to shape, position or colour alone. |
| 1.3.4 | Orientation | AA | Automated | The gate measures the workbench at five sizes down to 320 CSS pixels, including portrait shapes, with no orientation locked. |
| 1.3.5 | Identify Input Purpose | AA | Not applicable | The product collects no information about the person using it, so no input has a purpose from the WCAG list to identify. |
| 1.4.1 | Use of Color | A | Partly automated | The gate rule forced-colours decides whether a control survives the system replacing the palette. Whether meaning is carried by colour alone elsewhere is manual. |
| 1.4.2 | Audio Control | A | Manual | Emulator sound never starts on its own; a machine has to be run. Verified by a person starting the workbench and confirming silence. |
| 1.4.3 | Contrast (Minimum) | AA | Automated | The gate rule contrast measures every element that holds text against its own background, at 4.5:1 or 3:1 for large text. |
| 1.4.4 | Resize Text | AA | Automated | The gate measures the workbench at 200% and 400% zoom with nothing clipped or out of reach. |
| 1.4.5 | Images of Text | AA | Manual | Reviewed by confirming that every image in the interface and the help is a screenshot of the product or a diagram, never text rendered as a picture. |
| 1.4.10 | Reflow | AA | Automated | The gate measures at 320 CSS pixels with no horizontal scrolling, nothing past the viewport that cannot be scrolled to, and no control out of reach. |
| 1.4.11 | Non-text Contrast | AA | Partly automated | The gate rule forced-colours decides whether a control has a visible boundary. Contrast of the boundary itself against its background is manual. |
| 1.4.12 | Text Spacing | AA | Automated | The gate applies the spacing the criterion names and reports any element whose text then exceeds a box that cannot scroll. |
| 1.4.13 | Content on Hover or Focus | AA | Manual | Reviewed by confirming that every hover or focus panel can be dismissed without moving the pointer, stays while the pointer is over it, and persists until dismissed. |
| 2.1.1 | Keyboard | A | Partly automated | The gate rules keyboard reachability and pointer-alternatives decide that every group is reachable and that dragging and destructive actions have keyboard routes. Whether each route is usable is manual. |
| 2.1.2 | No Keyboard Trap | A | Automated | The gate rule keyboard reachability confirms focus can always leave wherever it is. |
| 2.1.4 | Character Key Shortcuts | A | Automated | Every workbench chord is declared in one binding table, every binding uses a modifier, and each can be changed or unbound in Settings. The table is contract-checked. |
| 2.2.1 | Timing Adjustable | A | Not applicable | Nothing in the product has a time limit. A build, a run and a test each continue until they finish or are stopped. |
| 2.2.2 | Pause, Stop, Hide | A | Automated | The gate rule reduced-motion reports anything animating for longer than a twentieth of a second once the preference is set, and nothing moves for five seconds unprompted. |
| 2.3.1 | Three Flashes or Below Threshold | A | Manual | The interface itself does not flash. An emulated machine can flash because the program being written makes it, which is the program author’s content rather than this product’s; the help says so. |
| 2.4.1 | Bypass Blocks | A | Manual | A skip link to the editor is present and is verified by a person following it from a cold load. |
| 2.4.2 | Page Titled | A | Automated | The gate rule document-title requires a non-empty title. |
| 2.4.3 | Focus Order | A | Manual | Judged by a person tabbing through each workspace. A rule can say every group is reachable; it cannot say the order is sensible. |
| 2.4.4 | Link Purpose (In Context) | A | Partly automated | The gate rule control-name decides that every link has a name. Whether the name describes where it goes is manual. |
| 2.4.5 | Multiple Ways | AA | Manual | The workbench is a single application rather than a set of pages, and offers the workspace rail, the command palette, project search and the go-to dialog as separate routes. Verified by a person. |
| 2.4.6 | Headings and Labels | AA | Partly automated | The gate rules heading-order and control-name decide that headings and labels exist. Whether each describes its content is manual. |
| 2.4.7 | Focus Visible | AA | Automated | The gate rule focus-visibility focuses every control and requires its appearance to change. |
| 2.4.11 | Focus Not Obscured (Minimum) | AA | Manual | Verified by a person tabbing with each panel and dialog open, confirming the focused control is never fully hidden behind a sticky element. |
| 2.5.1 | Pointer Gestures | A | Manual | The product uses no multipoint or path-based gesture. Verified by review of every pointer handler. |
| 2.5.2 | Pointer Cancellation | A | Manual | Verified by review that no action completes on pointer-down alone. |
| 2.5.3 | Label in Name | AA | Manual | Verified by comparing each visible label with the accessible name that carries it, so speech input matches what is written. |
| 2.5.4 | Motion Actuation | A | Not applicable | Nothing in the product is operated by moving or tilting the device, and no motion sensor is read at all. |
| 2.5.7 | Dragging Movements | AA | Automated | The gate rule pointer-alternatives requires anything draggable to declare its keyboard alternative, and refuses an alternative claimed without being stated. |
| 2.5.8 | Target Size (Minimum) | AA | Automated | The gate rule target-size measures every pointer target at 24 by 24 CSS pixels, with the essential exception declared in the markup where it is claimed. |
| 3.1.1 | Language of Page | A | Automated | The gate rule document-language requires a declared language. |
| 3.1.2 | Language of Parts | AA | Not applicable | The interface and help are written in one language throughout. Source code shown in the editor is not natural-language content. |
| 3.2.1 | On Focus | A | Manual | Verified by a person tabbing through each workspace confirming nothing changes context on focus alone. |
| 3.2.2 | On Input | A | Manual | Verified by a person changing each control and confirming nothing changes context without being asked. |
| 3.2.3 | Consistent Navigation | AA | Manual | The workspace rail and command palette are in the same place in every workspace. Verified by review. |
| 3.2.4 | Consistent Identification | AA | Manual | Verified by review that the same action carries the same name and icon everywhere it appears. |
| 3.2.6 | Consistent Help | A | Manual | In-app help is reached the same way from every workspace. Verified by review. |
| 3.3.1 | Error Identification | A | Manual | Every refusal in this product names what was refused and why, which is checked by the contracts on each module rather than by a page rule. |
| 3.3.2 | Labels or Instructions | A | Partly automated | The gate rule control-name decides that every control has a name. Whether the instruction is sufficient is manual. |
| 3.3.3 | Error Suggestion | AA | Manual | Refusals state the remedy as well as the reason. Checked by the contracts on each module and reviewed as prose. |
| 3.3.4 | Error Prevention (Legal, Financial, Data) | AA | Manual | Deleting a file goes to the project trash and can be undone; a revert asks for confirmation and says what it will discard. Verified by a person. |
| 3.3.7 | Redundant Entry | A | Not applicable | No process in the product asks for the same information twice, and nothing is entered across more than one step. |
| 3.3.8 | Accessible Authentication (Minimum) | AA | Not applicable | The product has no authentication step of any kind; nothing is signed in to and no cognitive function test is set. |
| 4.1.2 | Name, Role, Value | A | Partly automated | The gate rules control-name and duplicate-id decide that every control has an unambiguous name. Whether roles and values are announced correctly is manual, on each supported screen reader. |
| 4.1.3 | Status Messages | AA | Manual | Status regions are present throughout. Whether each announces at a useful moment is manual: a rule can see the region, not hear it. |

## What is not claimed

Level AAA is out of scope for this claim. Where the product happens to meet
a AAA criterion — honouring the reduced-motion preference, for one — it is
not counted here, because meeting one criterion of a level is not conformance
to it.

Screen-reader behaviour on each supported combination is the largest thing
that remains manual and unrecruited. Until representative testers have run
the manual matrix, this document records what is verified and by what, and
makes no overall conformance claim.
